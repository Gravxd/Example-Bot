const { getMember, formatDate, promptMessage } = require("../../functions.js"); // common ways to do commands
const { RichEmbed } = require("discord.js");
module.exports = {
    name: "commandname",
    run: async (client, message, args) => {


    }
}
